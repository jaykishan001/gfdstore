import React from 'react'

const OrderPage = async() => {
    // CHeck user if exist then only show orders other wise send it to login page
  return (
    <div>OrderPage</div>
  )
}

export default OrderPage