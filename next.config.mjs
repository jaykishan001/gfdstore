/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "loremflickr.com"], // Allow images from these domains
  },
};

export default nextConfig;
