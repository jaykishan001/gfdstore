/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "loremflickr.com", "res.cloudinary.com"], // Allow images from these domains
  },
};

export default nextConfig;
